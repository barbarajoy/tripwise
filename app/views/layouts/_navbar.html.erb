<nav>
  <div class="navbar-top navbar-height-<%= hauteur %>" >
    <a href="<%= root_path %>">
      <%= image_tag('logo.png', class: "img-navbar-height-#{hauteur}") %>
    </a>
    <div class="navbar-middle">
      <h2 class="navbar-heading">Find your next adventure</h2>
      <%= render "layouts/searchbar" if searchbar %>
    </div>
    <ul class="nav navbar-nav navbar-right ml-auto">
      <li id="navbar-link">
        <% if user_signed_in? %>
          <button type="button" class="button m-4">
            <%= link_to 'Sign out', destroy_user_session_path, data: {turbo_method: :delete} %>
          </button>
        <% else %>
          <button type="button" class="button m-4">
            <%= link_to 'Sign in/up', new_user_session_path %>
          </button>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
